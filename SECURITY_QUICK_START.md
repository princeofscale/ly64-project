# 🛡️ Краткое руководство по безопасности

## ✅ Что было добавлено

### 1. **Система логирования (Winston)**
Все события безопасности теперь записываются в файлы:
```
logs/
├── combined.log         # Все логи
├── error.log           # Только ошибки
└── security-audit.log  # События безопасности
```

**Что логируется:**
- ✅ Успешные входы
- ❌ Неудачные попытки входа
- 🚫 Заблокированные аккаунты
- 👤 Регистрации
- 🔐 Админские действия (удаление, обновление пользователей)
- ⚠️ SQL injection попытки
- ⚠️ XSS атаки
- 📊 Rate limit нарушения

### 2. **Улучшенная защита от взлома**

#### Защита от брутфорса:
- После **5 неудачных попыток** входа → аккаунт блокируется на **15 минут**
- Показывается количество оставшихся попыток
- Все попытки логируются

#### Rate Limiting (защита от DDoS):
- **Общий API**: 100 запросов / 15 минут
- **Логин**: 5 попыток / 15 минут
- **Регистрация**: 3 регистрации / час
- **Админка**: 200 запросов / 15 минут

#### SQL Injection защита:
- Автоматическое обнаружение паттернов: `SELECT`, `DROP`, `UNION`, `--`, и т.д.
- Логирование всех попыток
- Блокировка подозрительных запросов

#### XSS (Cross-Site Scripting) защита:
- Очистка всего пользовательского ввода
- Экранирование HTML тегов
- Нейтрализация `<script>` тагов

#### HTTP Security Headers (Helmet):
```
✅ Content-Security-Policy (защита от XSS)
✅ X-Frame-Options: DENY (защита от clickjacking)
✅ X-Content-Type-Options: nosniff
✅ Strict-Transport-Security (HTTPS)
✅ Referrer-Policy
```

### 3. **Безопасность JWT токенов**
- Явно указан алгоритм HS256
- Проверка issuer
- Требование минимум 32 символа для секрета
- Разделение ошибок (expired vs invalid)

### 4. **Аудит логирование админских действий**
Все действия админов записываются:
- Обновление пользователей
- Удаление пользователей
- Сброс диагностики
- С указанием: кто, что, когда, откуда (IP)

## 🔍 Мониторинг

### Просмотр логов безопасности:
```bash
# Windows
type logs\security-audit.log

# Последние 50 строк
powershell "Get-Content logs\security-audit.log -Tail 50"

# Поиск неудачных входов
findstr "LOGIN_FAILED" logs\security-audit.log
```

### Что смотреть:
- **LOGIN_FAILED** - неудачные попытки входа
- **LOGIN_BLOCKED** - заблокированные аккаунты
- **SQL_INJECTION_ATTEMPT** - попытки SQL инъекции
- **ADMIN_ACTION** - действия администраторов
- **RATE_LIMIT_EXCEEDED** - превышение лимитов

## ⚙️ Настройка для продакшена

### Обязательно перед деплоем:

1. **Сгенерируйте сильный JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

2. **Обновите `.env`:**
```bash
NODE_ENV=production
JWT_SECRET=<ваш-сгенерированный-секрет>
FRONTEND_URL=https://ваш-домен.com
PORT=3002
LOG_LEVEL=warn
```

3. **Включите HTTPS** (используйте Nginx/Apache с SSL сертификатом)

4. **Настройте автоматическую ротацию логов:**
   - Лог файлы автоматически ротируются при достижении 5MB
   - Хранятся последние 5 файлов для combined/error
   - Хранятся последние 10 файлов для security-audit

### Рекомендуется:
- Настроить мониторинг (Sentry, DataDog)
- Регулярные бэкапы БД
- Использовать Redis для rate limiting в кластерной среде
- Добавить 2FA для админов
- Настроить email уведомления для критических событий

## 📊 Тестирование безопасности

### Проверка зависимостей:
```bash
cd backend
npm audit

# Автоматическое исправление
npm audit fix
```

### Тест Rate Limiting:
```bash
# Попробуйте войти 6 раз с неправильным паролем
# Должна появиться блокировка на 15 минут
```

### Тест SQL Injection:
```bash
# Попробуйте ввести в email: admin'; DROP TABLE users;--
# Должно быть заблокировано с ошибкой
```

### Тест XSS:
```bash
# Попробуйте ввести в имя: <script>alert('XSS')</script>
# Должно быть очищено автоматически
```

## 🚨 Если обнаружена атака

1. **Проверьте security-audit.log** - найдите IP атакующего
2. **Заблокируйте IP** (временно или навсегда):
   ```typescript
   // В коде или через админку (если реализовано)
   addToBlacklist('123.45.67.89')
   ```
3. **Проверьте остальные логи** на предмет успешных атак
4. **Смените JWT_SECRET** если подозреваете компрометацию
5. **Уведомите пользователей** если были украдены данные

## 📈 Уровни безопасности

### Текущий уровень: ✅ **Базовая защита**
- Rate limiting
- XSS/SQL injection защита
- Логирование
- Account lockout
- JWT security

### Следующий уровень: ⏭️ **Продвинутая защита**
- 2FA (двухфакторная аутентификация)
- Email верификация
- CAPTCHA на критических эндпоинтах
- Redis для распределённого rate limiting
- IP геолокация и блокировка
- Honeypot ловушки

### Максимальный уровень: 🔒 **Энтерпрайз**
- WAF (Web Application Firewall)
- DDoS protection (Cloudflare)
- Penetration testing
- Bug bounty программа
- Compliance сертификация (SOC2, ISO 27001)

## 📞 Контакты

- Нашли уязвимость? **НЕ публикуйте в Issues!**
- Отправьте на: security@вашдомен.com
- PGP ключ: [добавьте если нужно]

---

**Помните:** Безопасность - это не разовая настройка, а постоянный процесс!

Последнее обновление: 2026-01-31
